<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:NLib.Wpf.Controls">
    <Style TargetType="{x:Type local:NPreFormHeader}">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:NPreFormHeader}">
                    <Grid>
                        <Border Background="Salmon"
                                Width="120"
                                Height="25" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <!--  DEFAULT COLORS  -->
        <Setter Property="Background" Value="{DynamicResource PageBG}" />
    </Style>
</ResourceDictionary>